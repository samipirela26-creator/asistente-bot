# Secretos y configuracion local (NUNCA subir al repo)
config.json

# Base de datos y sus archivos auxiliares de SQLite (WAL)
agenda.db
agenda.db-shm
agenda.db-wal

# Datos personales
respaldos/
tareas.json

# Lock anti-doble-instancia (efímero, por máquina)
.bot.lock

# Cache de Python
__pycache__/
*.pyc
.coverage
